name: tik
# kanarik: ak niekedy GitHub cron na tomto repe nabehne, tu to bude vidiet (raz za hodinu, zadarmo)
on:
  schedule:
    - cron: '5 * * * *'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  tik:
    runs-on: ubuntu-latest
    steps:
      - run: date -u
